# Everything visible from the API goes here!
# This is what we are going to interface to with JavaScript

# import * from './generated/prisma.graphql'

type Mutation {
  createItem(
    title: String,
    description: String,
    price: Int,
    image: String,
    largeImage: String,
  ): Item!
}

type Query {
  items: [Item]!  
}
